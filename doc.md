我是一名前端开发，我想借助AI编程工具，用Tauri做一个windows桌面应用(不考虑mac)，主要是为了方便我自己快速打开网页的需求，就像uTools中的网页快开插件功能
技术栈：React + TypeScript + Tailwind CSS，Tauri v2
核心交互：

- 全局快捷键 alt + space(可自定义)，打开一个宽800高50的悬浮输入框(或者叫搜索条)，自动获得焦点，窗口位于桌面正中间偏上的位置，窗口置顶,但不影响点击其他区域，无边框无阴影，有圆角。输入内容时，搜索条下方展开下拉选项且自动根据输入关键字过滤收藏中的url的title，默认预选中第一个，键盘上下键可预选其他项，按下enter键时，打开预选项的url，鼠标直接点击某一项时，打开该url。搜索条和下拉框背景都是浅灰色，但预选项背景色更深一些。下拉区域高度最大600，支持内部滚动
- 按esc键或点击搜索条外部区域关闭窗口，管理窗口是独立窗口，有自己关闭按钮，不受搜索条失焦影响
- 输入 /admin 后按 Enter 打开一个新的管理窗口（且该文本不再被当作网页过滤），里面是一个列表，展示所有收藏的url地址、title、修改时间(YYYY-MM-DD HH:mm:ss)。支持新增、删除、编辑、查询，也就说这里可以维护收藏的url。搜索：支持搜索标题或 URL。按添加时间排序，最近添加的在上面。底层就是维护一个json数据，类似这种

```json
[
  {
    "title": "网页A",
    "url": "https://www.baidu.com",
    "addTime": "2026-04-25 10:00:00"
  },
  {
    "title": "网页B",
    "url": "https://www.baidu.com",
    "addTime": "2026-04-25 10:00:00"
  },
  {
    "title": "网页C",
    "url": "https://www.baidu.com",
]
```

允许导入和导出json文件，导入时会覆盖原有数据，导出时会保存为json文件。导入 JSON 前做格式校验，防止损坏数据。

- 输入 /reload 回车，重启程序
- 系统托盘菜单：打开，管理，开机启动，退出，实现对应功能
- 生成免安装压缩包。数据文件路径放在exe 同级目录，也就是说如果我把这个文件夹复制到 U 盘或另一台电脑，双击 exe，所有收藏自动就绪，真正做到零残留、全绿色
- 管理窗口可以自定义全局快捷键，配置是否开机启动
- 输入搜索加 100ms 防抖: 不要 debounce 整个 input，只 debounce 搜索计算
- 调用系统默认浏览器打开，而非内置窗口
- 打开url(点击或enter)后，自动关闭窗口；按Esc时若搜索条有内容清空内容即可，若无内容，关闭窗口
- 修改收藏后，重启生效即可
  请你先规划任务，如有问题，请先告诉我
